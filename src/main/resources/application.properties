spring.application.name=property-management
#Allow H2 DB to be accessed via browser and via Java application
spring.h2.console.enabled=true
# this path is default path. Even if we don't provide this, it still works.
spring.h2.console.path=/h2-console
#DB url and DB name
spring.datasource.url = jdbc:h2:file:~/h2/propertydb
#DB username
spring.datasource.username=sa
#DB password
spring.datasource.password
#DB Driver
spring.datasource.driverClassName=org.h2.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect